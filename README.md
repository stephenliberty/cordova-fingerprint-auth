#Cordova fingerprint authentication

Currently just for android - there's already a great plugin written for iOS. 

Requests that a user confirms their ALREADY EXISTING fingerprint to authenticate themselves
as a device owner. 